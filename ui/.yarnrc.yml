nodeLinker: node-modules

# Allow dashboard builds from both Windows and WSL on the same checkout.
supportedArchitectures:
  os:
    - win32
    - linux
  cpu:
    - x64

yarnPath: .yarn/releases/yarn-4.1.1.cjs
